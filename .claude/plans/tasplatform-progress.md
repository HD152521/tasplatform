# TAS Platform 전환 — 진행 상태

**계획서**: `.claude/plans/tasplatform-split.plan.md`
**갱신**: 2026-09-08 · 1단계 done

이 파일이 유일한 진행 기록이다. PM 에이전트가 단계를 끝낼 때마다 여기를 고친다.
사람이 직접 고쳐도 된다 — 되돌리거나 건너뛰려면 `상태` 칸을 바꾸면 된다.

## 상태 값

| 값 | 뜻 |
|---|---|
| `pending` | 아직 시작 안 함 |
| `in-progress` | 작업 중 (중단되면 이 상태로 남는다) |
| `verifying` | 구현은 끝났고 검증 중 |
| `done` | 검증까지 통과 |
| `failed` | 검증 실패. `비고` 에 사유가 있다 |
| `blocked` | 사람 결정이 필요해 멈춤 |

## 단계

| # | 단계 | 상태 | 검증 | 비고 |
|---|---|---|---|---|
| 1 | playwright-java 실현 가능성 검증 | done | PM 직접 재실행 5건 통과 · 원본 세션 해시 불변 | 검증 셋 모두 가능. 쿠키 회전은 재현 안 됨(아래 참고) |
| 2 | Spring Boot 뼈대와 DB 이관 | pending | — | |
| 3 | 수집기를 Agent 로 이관 | pending | — | 1단계 통과 — 설계 변경 불필요. 세션 파일 소유권을 먼저 정할 것 |
| 4 | 읽기 API | pending | — | |
| 5 | React 프론트 뼈대와 읽기 화면 이관 | pending | — | |
| 6 | 브로드컴 쓰기 경로 | pending | — | 외부 쓰기. 개발 중에는 실제 전송 금지 |
| 7 | 지식 베이스 색인 | pending | — | 문서 소스 목록 미확정 |
| 8 | 챗봇 응답 (엄격한 출처 제한) | pending | — | |
| 9 | 채팅 화면 | pending | — | |
| 10 | 대화에서 Jira · Confluence · SR 만들기 | pending | — | 외부 쓰기. 개발 중에는 실제 등록 금지 |

## 사람 결정이 필요한 것

- [ ] **챗봇이 참조할 공식 기술 문서 사이트 목록** — 7단계 전까지 필요. 그전까지는 Jira·Confluence·SR 만으로 진행
- [ ] **세션 파일 소유권** — 지금은 broadcom-sr 가 원본을 갱신하고 tasPlatform_back 은 복사만 한다. 3단계에서 Agent 가 수집을 넘겨받을 때 누가 원본을 소유할지 정해야 한다
- [ ] **배포 위치** — 사내 TAS 후보. 컨테이너 디스크 휘발 · Chromium 실행 · 주기 실행 셋 확인 필요

## 기록

<!-- PM 에이전트가 단계를 끝낼 때마다 여기에 한 줄씩 덧붙인다 -->
- 2026-09-08 17:1x · **1단계 done**. playwright-java 로 셋 다 확인: browserContext.request() 쿠키 공유(대조군 401), storageState 저장·복원(쿠키 10개), Origin 주입(빼면 401). 케이스 목록 200·2건. PM 이 cleanTest 로 강제 재실행해 확인 — 첫 시도는 UP-TO-DATE 로 건너뛰어 증거가 안 됐다
- 2026-09-08 17:1x · 수집 스케줄 Ready 로 복구
- 2026-09-08 16:3x · 1단계 착수. **수집 스케줄을 Disabled 로 내림** — Java 쪽이 같은 세션을 만지면 쿠키 회전으로 세션이 죽는다. 1단계 끝나면 반드시 되돌린다
